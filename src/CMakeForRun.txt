cmake_minimum_required(VERSION 3.20)

project(PV204project)

set(CMAKE_CXX_STANDARD 17)

include_directories(include)
include_directories(lib)

set(FROST_RUST_LIB
        ${CMAKE_SOURCE_DIR}/lib/frostdemo/target/release
)

add_executable(node_app
        src/AlgorithmPlayground.cpp
        src/node.cpp
        src/coordinator.cpp
        src/server_main.cpp   # Your new HTTP server code
)

target_link_libraries(node_app
        ${FROST_RUST_LIB}/libfrostdemo.so
        sodium
        dl
        pthread
)

set_target_properties(node_app PROPERTIES
        BUILD_RPATH "${FROST_RUST_LIB}"
        INSTALL_RPATH "${FROST_RUST_LIB}"
)